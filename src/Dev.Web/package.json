{
  "name": "pixui_web",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --mode dev",
    "serve": "vite preview",
    "test": "vitest",
    "libSystem": "tsc && vite build --mode libSystem",
    "libPixUI": "tsc && vite build --mode libPixUI",
    "libCodeEditor": "tsc && vite build --mode libCodeEditor",
    "libLiveChartsCore": "tsc && vite build --mode libLiveChartsCore",
    "libLiveCharts": "tsc && vite build --mode libLiveCharts",
    "libAppBoxCore": "tsc && vite build --mode libAppBoxCore",
    "libAppBoxClient": "tsc && vite build --mode libAppBoxClient",
    "AppStudio": "tsc && vite build --mode AppStudio"
  },
  "dependencies": {
    "canvaskit-wasm": "^0.38.0",
    "ts-pattern": "^4.0.1",
    "web-tree-sitter": "^0.20.7"
  },
  "devDependencies": {
    "rollup-plugin-visualizer": "^5.9.0",
    "typescript": "^4.7.4",
    "vite": "3.2.5",
    "vitest": "^0.29.2"
  }
}
