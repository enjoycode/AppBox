import TsParser from 'web-tree-sitter'

export type {SyntaxNode, Point, Edit, Query, QueryCapture} from 'web-tree-sitter'

export class Parser extends TsParser {
}

// export interface SyntaxNode {
//     typeId: number;
// }
