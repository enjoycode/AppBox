
```csharp
new Button("管理员") { Enabled = sys.Permissions.Admin.State, OnTap = OnTap }


private async void OnTap(PointerEvent e)
{
    if (!(await sys.Permissions.查看大屏.Task))
    {
        Notification.Error("无权限");
        return;
    }

    Notification.Info("Done.");
}
```